module VIM
  class << self
    def evaluate(*args)
      # no need to work in test
    end

    def command(*args)
      # no need to work in test
    end
  end
end

require_relative "../plugin/url_generator"
